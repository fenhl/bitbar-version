[package]
name = "bitbar-version"
version = "0.1.0"
authors = ["Fenhl <fenhl@fenhl.net>"]
edition = "2018"

[[bin]]
name = "bitbar-version"
path = "src/main.rs"
test = false
bench = false

[dependencies]
plist = "0.5"
reqwest = "0.9" #TODO upgrade to 0.10 and rework to use async
semver = "0.9"

[dependencies.bitbar]
version = "0.3"
features = ["base64"]

[dependencies.serde]
version = "1"
features = ["derive"]
